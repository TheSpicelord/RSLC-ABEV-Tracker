# RSLC ABEV Tracker

Interactive map/table tracker of Absentee (AB) and Early Vote (EV) activity for the **November 3, 2026 general election**, by state legislative district. Built for RSLC political research. Derived from the sibling `RSLC-District-Explorer` project (same root folder) — map engine, styling, and architecture are ported from there.

## Running & Deployment

- **No build step** — pure ES6 modules, serve from any static host
- Local dev: serve from the **parent** `Coding Projects` folder (`python -m http.server 8000`), then open `http://localhost:8000/RSLC-ABEV-Tracker/`. Serving from this folder alone works, but the sibling District Explorer data (targets / past leg margins) won't resolve and will fall back to the hosted site.
- Cache busting: `?v=BUILD_VERSION` in **three** places, all of which must be bumped together — `index.html`'s CSS/JS tags, the `BUILD_VERSION` constant in `app.js`, and **`app.js`'s own `./modules/*.js` import specifiers**
  - **The module specifiers are the easy one to forget, and forgetting it is fatal, not cosmetic.**
    `index.html` busts `app.js`, but a bare `import ... from "./modules/config.js"` is a
    *separate* URL with no version on it, so the browser happily pairs a fresh `app.js`
    with a cached `modules/*.js`. If that release added a new export, the import fails,
    `app.js` never parses, and its imports are never even fetched — the page renders
    blank, with no partial degradation to hint at what happened. That is exactly what
    `STATE_DATA_NOTES` did on 2026-09-08: the request log showed `index.html`, `style.css`,
    `app.js`, and then nothing at all.
  - **`node --check file.js` does NOT catch this class of bug.** On a `.js` file Node
    parses as CommonJS and let an unterminated string literal through; the browser, using
    module semantics, rejected the same file. **Validate as a module**: `cp app.js
    /tmp/x.mjs && node --check /tmp/x.mjs`, which reports the exact line. Better still,
    fetch the bytes the browser will actually fetch (`curl "app.js?v=<version>"`) and parse
    those, so a stale-serve is caught too.
  - Diagnosis is quick: if the server log stops after `app.js` and never requests
    `modules/config.js`, it is this. A hard refresh clears it locally, but shipping the
    fix requires a **version bump**, because the broken `app.js` is itself cached under
    the old version and would otherwise keep being served.
. ABEV **data** JSON (`fetchJson`) instead busts with a fresh per-page-load token (`withDataBust`), so a normal reload shows the latest daily data push without a hard refresh; shapefiles + District Explorer data stay on `BUILD_VERSION` (large, rarely change)
- Hosted on GitHub Pages from `main` at https://github.com/TheSpicelord/RSLC-ABEV-Tracker

## The Four Stats

Three tracked stats + one calculated: `requested` (RequestDate), `returned` (ReturnDate), `ev` (EarlyVoted), and `voted` = returned + ev (computed client-side, never stored). Ballot-sent is deliberately NOT tracked. Each stat is broken into three modeled-party buckets: `rep`, `dem`, `toss`.

**SIGN CONVENTION — IMPORTANT:** Net = `rep − dem`. **Positive = GOP advantage** (red), negative = Dem (blue). This is *reversed* from District Explorer's DEM_MARGIN convention. Map fill uses `netColor(netPct)` where `netPct = net / total × 100`; the hue saturates at ±20.

## Architecture

```
index.html          # Entry point (topbar controls, map, sidebar)
app.js              # All map/UI logic (~1900 lines)
style.css           # District Explorer theme + "ABEV Tracker additions" section at bottom
modules/
  config.js         # Constants, stat views, AUTH_ENABLED flag, URLs
  dom.js            # DOM element references
  state.js          # Global state object (single source of truth)
  auth.js           # Password gate (Cloudflare Worker) — currently DISABLED via config
data/
  shapes/           # states/house/senate .zip shapefiles (from District Explorer)
  abev/             # GENERATED — do not hand-edit
    <state>_<chamber>.json, national.json, timeline.json, abev_files.json  # 2026 (daily_update.py)
    history/<year>/...  + history.json                                     # 2022/2024 (historical_pull.py)
scripts/
  daily_update.py           # 2026 SQL Server -> JSON -> git push (the whole daily pipeline)
  historical_pull.py        # one-time 2022/2024 general-election backfill (no push loop)
  db_config.template.ini    # Copy to db_config.ini (gitignored) with real creds
```

## SQL Source (VPN required)

Server `dtazclient1.gdtazdata.smartechcorp.net`, database `DTODD_RSLC`, SQL auth (creds in gitignored `scripts/db_config.ini`; driver "ODBC Driver 17 for SQL Server", Encrypt + TrustServerCertificate).

- `dbo.General_Absentees_2026` — one row per voter per election activity. Key columns: `State` (2-letter abbr), `RNC_RegID` (uniqueidentifier — join key to models via `CONVERT(varchar(36), ...)`), `SenateDistrict`/`LegislativeDistrict` (varchar district numbers; may be NULL or '0' = unmatched), `RequestDate`/`ReturnDate`/`EarlyVoted` (date). `BallotSentDate` exists but is unused.
- Model tables (per state, configured in `STATE_MODELS` in daily_update.py; all join on `dt_regid` = text GUID):
  - **VA**: `dbo.RSLC_VA_R2_Exchange_20250804` — `RepublicanFramework_Flag` / `PersuasionFramework_Flag` / `DemocratFramework_Flag` (mutually exclusive 0/1). Persuasion + unmatched → toss.
  - **WI**: `dbo.RSLC_WI_Exchange_20260819` — `Framework` = `'Rep'` → rep, `'Dem'` → dem, `'Pers'` + unmatched → toss. 9 universes (`universenumber`/`universename`), but bucket on **`Framework`, not a universe range** — see the MI note below.
  - **MI**: `dbo.RSLC_MI_R2_Exchange_20260805` — same Aug 2026 refresh format as WI: `Framework` = `'Rep'` / `'Dem'` / `'Pers'`. 8 universes, and they are **not numbered like WI's 9**: "Available Dems" is universe 7 (Pers) in WI and universe 6 (Dem) in MI, so a hardcoded 1–2 / 6–7 range that is right for one silently mis-buckets the other. `Framework` is nested exactly inside `universenumber` in both tables, so the two agree wherever the ranges are right — `Framework` is simply the one that stays right.
  - **AK**: `vs.ak_scores_audiences_20260721` (note the `vs` schema, not `dbo`) — statewide DSP model for the 2026 U.S. Senate race. `framework` = `'Sullivan'` → rep, `'Peltola'` → dem, `'Persuasion'` + unmatched → toss. ~92% matched on the AK feed. AK's permanent lean model — it served the Aug 18 primary and carries over unchanged to the Nov 3 general (the turnout-flag columns in the table are ignored — only `framework` is used).
  - **PA**: `dbo.PA_RSLC_R1_Exchange_20260418` — `UniverseNumber` 1–7: 1–2 rep, 6–7 dem, 3–5 (persuasion/swing incl. Dem Peel) + unmatched → toss.
  - **Ported from District Explorer** (same tables and bases, so a district's lean matches between the two projects — DE's list lives in `MODELS` in its `scripts/build_model_margins.py`):
    - **NV** `dbo.NV_GOV_IE_R1_Exchange_20260105` and **AZ** `dbo.RGA_AZ_R2_Exchange_20260121` — `universenumber` 1–2 rep, 6–7 dem.
    - **GA** `dbo.RSLC_GA_Exchange_20260721` — 9 universes, so the Dem base is **8–9**, not 6–7.
    - **NJ** `dbo.RSLC_NJ_Transfer_20250712` — 9 universes with three-deep bases: **1–3 rep, 7–9 dem**.
    - **TX** `dbo.RSLC_TX_Scores_TurnoutSupportAudiences_20260601` — Strong/Soft Republican and Democrat Supporter audiences. These are **varchar `'1'`/`'0'`**; an unquoted `= 1` matches nothing.
    - **IA** `dbo.ia_scores_audiences_20260731` — `framework_lahn` (rep) / `framework_sand` (dem), int flags.
    - **OR** `dbo.or_audience_flags_20200727` — `state_leg_ballot_rep_audience` / `_dem_audience`. DE carries a governor-ballot model beside this one; a legislative tracker takes the **legislative** ballot. Note the **uppercase `DT_REGID`** join column, unique to this table.
  - **National fallback** (`NATIONAL_MODEL_TABLE`): `dbo.[RSLC DRA June National Audiences and Scores]` — 227M rows, one per `dt_regid`. `RSLC Republican / Democratic / Swing Legislative Voters` are mutually exclusive '1'/'0' flags; swing + unmatched → toss. Used by any state with no exchange file of its own (currently RI, **NC**, **IL**, **WV**, **MD**, **DE**, **CT** and **NY**; ~95% match rate). **WV / MD / DE were wired 2026-09-05 for the backfill only** — none is in `ACTIVE_STATES` and none has 2026 feed rows yet. Coverage is in line with every other fallback state: WV 91.0% / 96.3%, MD 91.7% / 95.9%, DE 90.2% / 95.1% for 2022 / 2024. All three came back clean on the redistricting check (no district under 80% retention, medians 95-99%), so none needed `HISTORY_STALE_DISTRICTS` entries. **IA passed through it for a few hours on 2026-09-05 and is now off it again** — see the IA note below.
    - **IA runs on `vs.IA_scores_audiences_20260731_V2`, bucketed on the universe ladder, not its framework flags.** The V2 refresh landed 2026-09-05 and is a real full-file model: 2,148,056 rows, one per `dt_regid`, against roughly 2.2M registered Iowans, matching **91.3%** of the 2022 absentee feed and **96.1%** of 2024 — edging the national fallback (90.8% / 95.6%) while being purpose-built for the 2026 race. It replaced V1 (`dbo.ia_scores_audiences_20260731`), a *persuasion subset* of 354,382 rows that matched only 15% of the feed and dumped ~84% of absentee voters into `toss`; V1's failure is why IA briefly sat on the national fallback earlier the same day. **Bucket 1-2 / 8-9 like GA, never on `framework_lahn` / `framework_sand`.** V2 carries both, but the flags are asymmetric — `framework_lahn` is universe 1 alone (Lahn Base) while `framework_sand` spans 7-9 — so they silently drop universe 2 "Republican Targets" (262,135 voters, ~40k of the 2024 absentee feed) into `toss` while keeping the mirror-image Dem universe. That is **7.9 points of margin** on 2024 (R-5.8 on universes vs R-13.7 on flags). The ladder's names mirror cleanly, so the GA split is the right one. History for IA was re-pulled on V2 the same day.
- **NC has no usable state model and is on the national fallback by design.** The only
    NC file on the server, `dbo.NC_Legislative_GOP_UAF_Scores_Audiences`, is a *GOP
    targeting* file, not a partisan classification. Its five audiences are clean (every
    row is in exactly one; `dt_regid` unique; float flags, so `= 1` works), but they split
    3.2M "GOP" to 1.3M "DEM" in an even state; it covers only **66.8%** of the NC absentee
    feed where every other state model runs 90-99%; and the misses are *partisan* — urban
    Dem districts match ~44-50%, rural GOP ones ~80%. Bucketing on it puts the 2024 NC
    absentee electorate at **R+29.7**. The national model matches 97.5% of that same feed
    uniformly (97-99% in every senate district) and lands it at **R+3.9**, against an
    actual Trump +3.2. Don't "upgrade" NC to the state file; if a real RSLC NC exchange
    table ever appears, swap it in and index it.
- Models are hard classifications (buckets), NOT likelihood scores. Future state models follow the same pattern.
- **Model tables are indexed on `dt_regid`** (covering index INCLUDEing the bucket columns) so the daily aggregate seeks instead of scanning — see `scripts/sql/create_model_indexes.sql`. Model tables are static (a refresh arrives as a brand-new table), so the index persists. **When you point a `STATE_MODELS` entry at a new/refreshed model table, add its index to that file and re-run it**, or that state goes back to a full scan. The AB feed tables are externally owned and deliberately left unindexed.
  - The file also has a **retired-index section at the end** that DROPs indexes on tables no longer in `STATE_MODELS`. Add a DROP there in the same edit that adds a new CREATE — a model swap otherwise leaves the old table's index behind forever. Cleaned up 2026-09-08: dropped the orphans on `ia_scores_audiences_20260731` (V1), `MI_SEN_IE_R1_Exchange_updated_20260507` and `RGA_WI_ExchangeData_20260131`, reclaiming 740.6 MB.
  - **A join column that cannot be indexed gets an INDEXED PROJECTION instead** (added 2026-09-09 for NH and OH). Both VS audience files key on `rnc_reg_id nvarchar(MAX)`, which SQL Server refuses as an index key (error 1919) even though every value is exactly 36 characters, so every aggregate scanned the whole vendor table. `create_model_indexes.sql` now builds `dbo.<table>_idx` — `dt_regid` cast to `varchar(36)` plus only the audience columns, clustered on `dt_regid` — and `STATE_MODELS` reads that. **Ohio 60.1s → 25.3s (2.4x), New Hampshire 9.2s → 6.8s, byte-identical output** (verified by re-running both backfills and diffing: no change). A copy rather than `ALTER COLUMN` on the source, because the source is vendor-supplied and other consumers may read it. **A projection goes stale if the vendor replaces the source** — drop it and re-run this file; the build is guarded on existence so re-running is cheap.
  - **Where daily-update time actually goes** (measured 2026-09-09): the model-side join, which is what all of this addresses. The AB feed itself is a heap with no indexes and does not need one — a per-state scan of the 6.5M-row 2026 feed costs **0.2s**, and the watermark queries 0.1–0.2s each. For comparison, an *indexed* model aggregate runs ~8.9µs per feed row (AZ: 3.62M rows in 32s) against ~23µs unindexed (OH before the projection). If a daily run ever gets slow again, time the aggregates first — not the feed.
  - **District Explorer would benefit too, but differently.** Its builders join `TRY_CONVERT(uniqueidentifier, m.[col]) = v.RNC_Regid`, which is non-sargable on the model side and so scans regardless of any index. Pointing it at the narrow projections would still help (7 columns instead of 77), but its builds are manual and infrequent, so this has not been done.
  - **The IA V2 move on 2026-09-05 missed this file**, so Iowa spent three days full-scanning 2.15M rows against a stale index on the 354k-row V1 table. Fixed 2026-09-08. Audit is one query: every table in `STATE_MODELS` should appear in `sys.indexes` with an `IX_dtregid_*`, and nothing else should.
- **`STATE_MODELS` and `ACTIVE_STATES` are separate questions.** A state needs both a model *and* rows in `dbo.General_Absentees_2026` before it can be published. As of 2026-09-05 the feed carries FL, VA, PA, NJ, IL, WI, MN, GA, AK, RI — **FL is new since 09-01 and is now the largest state in it**, with no model yet. Formerly: **PA/NJ/GA are wired and ready to activate**; **IL/MN** have data but no model (they would work on the national fallback, like RI); **NV/AZ/MI/TX/IA/OR** have models but no feed rows yet. Adding a state with no feed rows produces an empty state, not an error — and since 2026-09-05 `build_outputs()` **omits a zero-activity state from `national.json` / `timeline.json` entirely**, so a state can be activated *ahead* of its data and stays invisible until the vendor delivers it. That is how NC is wired right now.
- `ACTIVE_STATES` now carries **every state with rows in `dbo.General_Absentees_2026`** — 12 as of 2026-09-09 (FL, VA, PA, NJ, IL, WI, MN, GA, AK, RI, IA, NC) — plus KS, which is wired and waiting on its data. **A full forced pull of all 13 takes 1m45s** at `--workers 6` (measured 2026-09-09), so there is no reason to hold a state back for cost. FL and MN were the last two to need a model; both are on the national fallback (PA/NJ/GA added 2026-09-01, NC 2026-09-05, KS 2026-09-08). **KS** buckets on `dbo.RAGA_KS_Exchange_20260708`, universes **1-2 rep / 7-9 dem** — asymmetric on purpose, as specified by the model's owner: universe 7 "Available Democrats" counts as Dem while its mirror, universe 3 "Trump 2024 Overperform", counts as neither (~1.6 points toward Dem versus a symmetric split). Shared with District Explorer's `MODELS["KS"]`, which replaced a dropped-in workbook with this same table on 2026-09-08 — and in doing so moved every Kansas district ~10 points more Republican, so the two projects agree with each other but not with anything published from the old workbook. **NC was activated with zero feed rows** — its ballots went out 9/4 but the vendor had not loaded it yet — so it publishes itself on the first daily run after the data lands; its 2022/2024 backfill is already in place and does not wait on the 2026 feed. NC has no `ABEV_START_OVERRIDES` entry, so the chrono/trend window starts at its first date with data (the overrides exist for perm-AB-list states, which NC is not). **The VA and WI spring test data was retired 2026-09-09.** The 2026 feed carries an `ElectionType` per row, and off-cycle contests share the table with the general: VA's March referendum is `VA Special Election` (1,503,657 rows) and WI's April Supreme Court race is `WI Spring General Election` (428,058). Every 2026 query now filters to `ElectionType = '<ST> General Election'`, so **the spring rows were never deleted — they are simply not counted**, and VA and WI light up on their own when November data lands, with no code change. Both stay in `ACTIVE_STATES`; the zero-activity guard keeps them off the site meanwhile. The match is **exact, not `LIKE '% General Election'`**, because "WI Spring General Election" passes a suffix match; `pull_state` prints what a state's feed actually holds when the general filter returns nothing, so a differently-labelled general cannot fail silently. Their `election_day` overrides and their `ELECTION_DAY_OVERRIDES` / `ABEV_START_OVERRIDES` / `STATE_DATA_NOTES` entries are all gone, and the stale `va_*`/`wi_*` chamber files were deleted. Historical VA/WI (2022/2024) is untouched. Formerly: VA/WI were the spring test states (VA March referendum, WI Supreme Court race) and have full request/return/EV data. **AK** runs on the General feed / Nov 3 like any other state, with lean from the DSP model — its temporary Aug 18 primary bolt-on was retired after that election, so no state currently overrides `abev_table` / `extra_where` / `election_day`. Those hooks stay in `STATE_MODELS` for the next primary; the MI and AK bolt-ons in git history are the worked examples of using them, and retiring one means removing the entry's overrides, the state's `"02"`-style `ELECTION_DAY_OVERRIDES` / `ABEV_START_OVERRIDES` entries in config.js, and the primary numbers left in `data/abev/`. **RI is request-only** — every row is a permanent-absentee list signup, zero returns and zero EV, so its Returned/EV/Total views are all zeroes by design (real Nov 3 election day).
- **AK has no `SenateDistrict`** in the feed (all `'0'`). `alaska_senate_from_house()` derives it: Alaska builds each senate district from two consecutive house districts (A = HD 1–2, B = 3–4, … T = 39–40), matching the shapefile's `00A`–`00T` `SLDUST` values. A state model may set `derive_senate` to any `hd_id -> sd_id` callable to do the same.

### Date edge cases (handled in `timeline_key()`)

- **Any event dated before `EARLIER_CUTOFF` = Sept 1, 2026 → timeline bucket `"earlier"`**, for all three stats. Before the window opens the feed is permanent-absentee list signups (some from 2021: PA 938k, NJ 872k, IL 444k) plus scattered bad dates (a PA return dated 4/22, an MN return dated 8/13), and none of it earns a row of its own in the day-by-day table. Changed 2026-09-10 from a Jan 1 cutoff that folded only *requests*; that version emitted ~230 dated rows per state, and `timeline.json` fell 100KB → 4.2KB / 2,190 rows → 74 when it was moved. Still counted in every total — verified statewide sums reconcile against `national.json` after the change.
- Dates in the future or past election day, or NULL → timeline bucket `"unknown"`
- Because unrefreshed states are rebuilt from disk by `load_prior_result`, a cutoff change only takes effect for states that actually re-pull — **run `--force` after moving it**, and note `timeline_rows` sorts any non-ISO key first so a stale `"pre2026"` still lands in the right place.
- All rows still count in district/state **totals** regardless of date validity — the buckets only affect the chronological breakdown

## Data Pipeline

Daily flow (on a VPN-connected workstation): `python scripts/daily_update.py`
1. One aggregate query per active state: LEFT JOIN model table, bucket by model, `GROUP BY district pair × stat × bucket × event_date` **server-side** (~100–200k summary rows/state; no individual voter data ever leaves the server)
2. Python rolls up: per-chamber district totals, statewide totals (includes district-unmatched voters), per-date statewide timeline
3. Writes `data/abev/*.json`, then `git add data/abev && commit && push` → GitHub Pages redeploys

Flags: `--no-push` (files only), `--dry-run` (query + summary, no writes), `--states VA,WI`.

### Historical backfill (2022 / 2024)

`python scripts/historical_pull.py` — one-shot (no git push), pulls past **general-election** ABEV from `dbo.General_Absentees_2022` / `_2024`, matching the **same** 2026 state models (moved-away/dead voters simply don't match → `toss`, like any 2026 non-match). Writes the identical record shape under `data/abev/history/<year>/` plus a `history.json` index; the 2026 files are untouched. Flags: `--years 2022,2024`, `--states VA,WI`, `--dry-run`. Backfilled so far (37): **VA, WI, PA, NC, MI, IA, AK, GA, NJ, TX, OR, RI, IL, WV, MD, DE, CT, NY, KS, ND, SD, NE, IN, KY, TN, MT, ID, WY, CA, WA, NH, OH, MO, AR, AZ, NM, UT** — 36 states in each year (KY is 2022-only, WY 2024-only) — MI/IA added 2026-09-05, AK/GA/NJ/TX/OR the same day; KS 2026-09-08 on its new RAGA SQL model; ND/SD/NE, IN/KY/TN and MT/ID/WY 2026-09-08, CA/WA 2026-09-09, all on the national fallback. Still unpulled: **NV, AZ**, plus **MN, CO** which are deliberately on hold until their state models land in SQL Server (expected soon, per the 2026-09-05 direction) - pulling them on the national fallback now would only have to be redone.

- **ND / SD need MD's `hd_sql` subdistrict treatment; NE is unicameral.** ND splits House district 4 into 4A/4B (Fort Berthold) and SD splits 26 and 28 into A/B halves, both to protect tribal voting strength — but their absentee feeds carry only the plain district number, with `LegislativeDistrict` byte-identical to `SenateDistrict`. Without `hd_sql` those voters land on "004"/"026"/"028", which exist in no chamber file, and 2 ND + 4 SD districts come out empty. `voterfile_2026.StateLegLowerSubDistrict` supplies the letter under the same two rules as MD. Verified: both states' house AND senate district id sets are an **exact match** against District Explorer's, lettered ids included. **NE** has `LegislativeDistrict = '0'` on every row, so it produces no house rollup and no `ne_house.json` — correct, since DE has no `ne_house.json` either and its 49 seats are the senate chamber. historical_pull's "every LEG district is NULL/0" flag is **expected** for NE.
- **ND 2022 sits partly on retired lines.** The Jan 2024 remedial order in *Turtle Mountain Band of Chippewa Indians v. Howe* changed **three** of ND's 47 districts for 2024, which are the lines still used in 2026. The other 44 are directly comparable, so ND is deliberately **not** in `HISTORY_STALE_LINES` — a whole-state N/A would hide 44 good districts to protect 3. One line in config.js flips that if the 3 turn out to matter more than the 44. SD (2021 Sparrow map) and NE (2021 map) have had no mid-decade redraw, so their 2022 is fully comparable.
- **Arizona moved to `dbo.RSLC_AZ_Exchange_20260819` on 2026-09-09**, replacing `RGA_AZ_R2_Exchange_20260121`, and now buckets on **`Framework`** like WI/MI rather than a universe range. That is not cosmetic: the old config used universes 6-7 for Dem, but this ladder puts "Available Dems" at 7 and the Democrat base at **9**, so carrying the range forward would have dropped universes 8 and 9 — over 2.2M Democrat-framework voters — into `toss`. The old index was dropped and a new one created in the same edit. Shared with District Explorer's `MODELS["AZ"]`.
- **Utah is on the national fallback**, 75/29 matching District Explorer, match 91.4%/94.4%, 1.4%/2.4% NULL districts. Its legislative maps (SB 2006 senate, HB 2005 house, Nov 2021) are unchanged and unlitigated — the *League of Women Voters* case that voided Utah's map was **congressional only** — so 2022 is directly comparable and no stale-lines entry is needed. Utah votes almost entirely by mail, so its early-vote column is genuinely small (31,917 in 2024 against 1.5M requests) rather than missing.
- **New Mexico is on the national fallback and is the cleanest feed in the set** — **0.0%** NULL districts in both years, 70/42 matching District Explorer exactly, match 92.7%/96.3%. Nothing about it needs a data note.
- **Minnesota's house ids are LETTERED and its feed writes them UNPADDED.** MN elects two representatives per senate district as `01A`/`01B` through `67A`/`67B`, but the feed says `"1A"` where every chamber file says `"01A"`, so **18 of its 134 house districts** — 1A/1B through 9A/9B — would have joined to nothing. `normalize_district_id()` now pads the numeric part of a lettered id to two digits, which is the width every lettered state already uses (MN `01A`, MD `27C`, ND `04A`, SD `26A`) and which leaves Alaska's derived `00A`–`00T` senate untouched. This is the same class of bug as California's `0001`, and the same check caught it: diff the pulled ids against District Explorer's before trusting a new state.
- **Missouri is REQUEST-ONLY, and is the exception `MIN_HISTORY_VOTES` anticipated.** `ReturnDate` and `EarlyVoted` are NULL on **100%** of its rows — all 949,818 in 2024 and 270,871 in 2022 — so its Returned/EV/Total views are zero and only Requested carries anything. The vote threshold would have dropped both years; `REQUEST_ONLY_STATES` in `historical_pull.py` exempts it. **Consistency is what separates this from a broken export**: WY 2022 had 62 returns against a healthy 2024, which is corruption, while Missouri is identical across two years and 1.2M rows, which reads as the vendor's scope. It is the mirror of TN (early votes only) and gets the same treatment — published, with a note so the zeros read as "not collected" rather than "no turnout".
- **AR 2024 loses 11.3% of its rows to a NULL district** (99,519 of 881,367), against 2.4% in 2022, so its district counts sum well short of statewide. Otherwise clean: 100/100 house and 35/35 senate both years.
- **Ohio buckets on the GENERIC CONGRESSIONAL ballot** (`cong_ballot_generic_rep_audience` vs `cong_ballot_generic_dem_audience`) from `VS.OH_Audiences_20260812`. District Explorer carries three races off this same table — US Sen (Husted/Brown), Gov (Ramaswamy/Acton) and Con — so an Ohio lean here matches DE's "OH Con" column. The file also has `cong_ballot_combined_rep/dem`; generic is the one chosen. Mutually exclusive but not exhaustive: 9.4% are in neither audience and fall to `toss`. Joins 96.3% of the 2024 OH feed. **Not indexed**, for the same `nvarchar(MAX)` reason as NH.
- **New Hampshire buckets on the GOVERNOR ballot** (`gov_ballot_named_ayotte_audience` vs `gov_ballot_named_dem_audience`) from `VS.NH_Audiences_20260812`, chosen 2026-09-09. The same file carries the US Senate ballot, and District Explorer shows **both** as side-by-side columns, so an NH lean here matches DE's "SUN Gov" and not its "SUN US Sen". Switching races is a two-word edit in `STATE_MODELS["NH"]`. The pair is mutually exclusive but not exhaustive — 9.7% are in neither audience and correctly fall to `toss`.
- **NH cannot be indexed, and does not need to be.** Its join column `rnc_reg_id` is `nvarchar(MAX)`, which SQL Server refuses as an index key (error 1919) even though every value is exactly 36 characters. At 916,682 rows it is the smallest model table here, so the scan is cheap. The fix, if it ever matters, is `ALTER COLUMN rnc_reg_id nvarchar(36)` — a schema change to a vendor table, so it should be agreed rather than done in passing. NH is the only `STATE_MODELS` entry with no index, deliberately.
- **NH house ids are COUNTY-CODED, so a numeric cap cannot validate them.** `601` is Merrimack 1 and Merrimack has 30 seats, but `740` and `908` are perfectly valid, so no single ceiling works. `CHAMBER_DISTRICT_ALLOWLISTS` takes a set instead, built from `NH_HOUSE_SEATS_BY_COUNTY`; the generated set comes out **exactly equal to DE's 203 ids**, which is what validates the per-county counts. This exists because the 2022 feed carries **`642` — Merrimack 42, in a 30-seat county — for 394 voters**, a bad source value that would otherwise have passed a numeric check and become a phantom.
- **NH's 39 floterial districts are COMPUTED from their base districts** (2026-09-09), so the house is complete rather than stopping at 163/203. A floterial has no territory of its own — it overlays whole base districts and elects extra members across them — and every source records one lower district per voter, so it can never be read directly. `scripts/nh_floterials.py` holds the mapping and does the rollup; `pull_state`/`pull_state_year` call it for NH only. The addition is **exact integer summation**, since no voter is in two of a floterial's parts, and timelines add per date so the chronological views stay consistent with the totals. **The file is a verbatim copy of District Explorer's** — each project keeps its own rather than importing across repos, so re-copy it if either changes.
  - Floterials are **tagged `"floterial": true`** in the emitted district records (2026-09-09), so anything that sums the column can skip them without knowing which ids they are. Verified: summing only the untagged districts comes in at or below statewide in both years, the remainder being voters with no district at all (5.5% / 7.4%).
  - **NH house districts deliberately sum to MORE than the statewide total** (2024: 269,067 across districts vs 180,597 statewide). That is not double counting in the wrong sense — a New Hampshire voter really is represented by both a base district and a floterial. Statewide totals come from `statewide`, never from summing districts, so nothing downstream is affected; just don't add the district column up and expect the state number. NH also has **no in-person early voting**, so its EV is zero by law, and 5-8% of its rows carry no district at all.
- **ALWAYS diff a new state's district ids against District Explorer's after a pull.** This is the check that catches silent joins-to-nothing, and it found a real one in California: the 2024 feed carries **both `"1"` and `"0001"`** for Assembly District 1, and `normalize_district_id()` used to zero-pad the raw string, so `"0001"` stayed four characters and became a phantom district matching no shapefile — **76,199 requests and 56,495 returns, about a third of AD-1's votes, rendering nowhere**. The normaliser now goes through `int()`, which collapses any padding and also treats `"00"`/`"000"` as the no-district sentinel instead of inventing district `"000"`. An audit of all 114 history files found this was the only occurrence. Use DE's `chamber_files.json` for the file names, never guess them — MI and MN are `michigan_*` / `minnesota_*`.
- **Washington's 2022 is a PARTIAL redraw — 14 districts.** Its 2021 commission map was struck under the VRA in *Soto Palmer v. Hobbs*; the court adopted Remedial Map 3B on 2024-03-15 (effective 03-28), still in use for 2026. Washington's senate and house share one set of 49 boundaries, so the retention test produced the **same 14 districts for both chambers** — a useful independent check. Six sub-90% districts (11, 36, 37, 43, 46, 48) are deliberately excluded as King/Pierce churn, and 40/42 are excluded despite a concentrated mutual exchange because Whatcom/Skagit are not among the court's affected counties. Every one of the 14 kept maps to a county that is. Reporting said "13 districts"; the measurement says 14, and the weakest case on county grounds (district 7) has one of the strongest movement signals. **CA needs no entry** — Prop 50 was congressional-only, so its 2022 and 2024 are both on current lines.
- **WA is all vote-by-mail**: 13,991 early votes against 5.0M rows in 2024 (0.3%). Real, not a gap. **CA matches the national model at only 82.5% in 2022** (90.4% in 2024), so 2022's Swing bucket carries ~3.8M extra voters and its margin is damped relative to 2024.
- **Montana's 2022 is on retired lines** — the one redistricting case in this batch. Montana's commission did not adopt its post-2020-census legislative maps until **2023-02-22**, too late for that cycle, so 2022 ran on the old 2013 districts and 2024 was the first vote on the current lines. District *numbers* are unchanged (100 house / 50 senate either way), so nothing about a 2022 Montana row looks wrong — it is simply a different map. MT is in `HISTORY_STALE_LINES[2022]` with a `LEG_REDISTRICTING_NOTES["30"]` footnote, and District Explorer drops its `leg_2022` via `LEG_REDISTRICTED` (which cost the 25 senate districts that were up in 2022 their only leg margin — correct, since it belonged to a different map). ID (2021 maps) and WY (HB 100, March 2022) are stable across all three cycles.
- **MT has no early-vote rows at all**, in either year. Unlike TN or OR this is not a rule of the state — Montana allows in-person absentee from 30 days out — so it is a feed gap, and MT's AB Requested/Returned are complete and healthy.
- **ID's 2024 early-vote column was never delivered** (0 rows, against 51,459 in 2022). Idaho did hold early voting in 2024. ID also leaves the senate district blank on 4.4% of 2024 / 2.2% of 2022 rows despite its senate and house sharing one district number, so those voters are missing from the senate rollup only.
- **WY 2022 was DROPPED, not loaded** — 57,634 requests against **62** returns and zero early votes. See `MIN_HISTORY_VOTES` below. 2024 is healthy (37,563 / 34,499 / 81,211), so this is specific to 2022. Wyoming also has the **lowest model match of any backfilled state, 78-82%** (most run 88-98%), so its Swing bucket is inflated and its margin correspondingly damped.
- **`MIN_HISTORY_VOTES` (5,000) drops a state-year with essentially no votes.** A year like WY 2022 cannot support anything the UI offers — On This Day, Final Results and the trend lines all compare vote totals — so it is omitted entirely rather than published as a 99.9%-empty column. Measured on **returned + ev, not requests**, because a broken export usually still carries its request rows. The floor is far below anything real: across all 28 backfilled states the smallest genuine state-year is **AK 2022 at 92,864 votes**, 15x the threshold, and the next smallest after Wyoming's 62 is that same 92,864 — there is nothing near the line. It deliberately does **not** catch a stat that is zero by design (TN's requests, OR/MT's early votes, PA/MI/CT's 2022 early votes); those all have hundreds of thousands of votes elsewhere. A genuinely request-only *historical* year would be caught and would need an explicit exception. Suppression also **deletes any chamber files left on disk**, because the index is rebuilt by globbing the year's directory and a stale file would keep the year half-alive.
- **KY, and only KY, has no 2024 data at all.** `General_Absentees_2024` carries 49 states and Kentucky is not among them (2022 carries 47, missing MA/MS). So KY backfills **2022 only** and its 2024 columns are absent rather than zero. That is a vendor gap, not a pull failure — re-pull KY for 2024 if it ever lands. `build_year_outputs()` **omits a state with no rows for a year** (added 2026-09-08, mirroring `daily_update.build_outputs`), so KY is simply missing from 2024's national/timeline rather than sitting there as an all-zero row asserting Kentucky had no absentee voting. The guard `pop()`s as well as skips, because `states_by_abbr` is seeded from the file on disk — without that, a stale zero row would survive every future re-run.
- **KY 2022 has real totals but NO usable chronology.** Verified at the source: every one of its rows carries the *same* date — `RequestDate` and `ReturnDate` are 2022-11-08 on all 75,118 mail rows (one distinct value each), and `EarlyVoted` is 2022-11-05 on all 267,916 early-vote rows (one distinct value). Those are placeholders (election day, and the last day of early voting), not real event dates. Consequences: KY's Requested and Returned views are necessarily identical, and its chrono/trend views collapse to a single spike instead of a curve. **District totals and margins are sound; only the time axis is meaningless.** Contrast IN 2022, which has 241 distinct return dates, and TN 2024, which has 10.
- **TN is early-vote only, by design.** `RequestDate` and `ReturnDate` are NULL on every one of its 2.2M (2024) and 882k (2022) rows: Tennessee has no no-excuse absentee and the feed carries only early in-person. TN's Requested/Returned views are therefore zero the way **RI**'s Returned/EV views are zero — the two are mirror images, and neither is missing data. TN's caps of (99, 33) also drop the `0` sentinel its 2022 feed carries.
- **ND 2024 drops 8.7% of rows to a NULL/0 district** (16,833 of 194,228; 2022 is 3.6%). Those voters still count in ND's statewide totals but land in no district, so ND's district counts sum short of its statewide number by that much. **FL** is the other big gap: 1.89M rows in the 2026 feed, the largest state in it, and still no model of any kind.
- **Two bugs the AK/GA pull exposed, both fixed in `historical_pull.py`.** (1) **`derive_senate` was never applied on the historical path**, only in `daily_update.py`. Alaska's feed has no `SenateDistrict` at all — NULL on all 160,181 rows in 2024 — so AK backfilled 40 house districts and an empty senate until the hook was ported across. (2) **`normalize_district_id()` has no range check**, and the historical tables carry sentinels the 2026 feed does not: GA alone contributes `000`, `999`, `99999` and a senate `099`, which became districts, giving GA 183 house / 59 senate instead of 180 / 56. `CHAMBER_DISTRICT_CAPS` + `valid_district()` now drop numeric ids outside a chamber's real range; lettered ids (AK senate) pass through untouched.
- **MA is BLOCKED and deliberately not loaded** (attempted 2026-09-06). Three separate problems, any one of which would stop it. (1) **There are no MA rows in `General_Absentees_2022` at all** - the state is 2024-only. (2) The feed's district columns are **arbitrary internal codes**, not district numbers: MA house values run 0-337 and senate 0-334, against DE's `001`-`160` and `D01`-`D40`, so nothing joins. (3) The senate cannot be mapped at all - DE's `ma_senate.json` carries `district_name: ""` for every record, so there is no name to match `voterfile_2026.StateLegUpperDistrict_Proper` ("PLYMOUTH AND BARNSTABLE") against; the only identifying field left is the incumbent's name. **The house alone is solvable**: `StateLegLowerDistrict_Proper` gives names like "THIRD BARNSTABLE" that map onto DE's "3rd Barnstable District" for **159 of 160** via ordinal-word conversion, the holdout being "BARNSTABLE DUKES AND NANTUCKET", which carries no ordinal and needs a special case. That would still leave MA house-only and 2024-only. Do not ship MA until the senate has a usable key.
- **NY was partially redrawn and CT was not.** NY's Assembly plan was replaced for 2024 after *Hoffmann v. IRC* and the Senate lines moved with it: Senate **6-9** (the Nassau cluster) fail unmistakably at 48-84% retention with 46-91% concentration, and Assembly **30, 42, 115, 132** fail on retention alone (56-78%), with **34, 39, 133** promoted at 83-89% retention plus 40-53% concentration. The wider NYC tail at 82-91% with scattered movers is **deliberately excluded** - same dense-city false positive Illinois exposed. **Checked 2022 -> 2024 only**: NY has no 2026 feed rows, so whether the 2024 lines carry into 2026 is unverified. CT came back clean in both chambers. **CT 2022 has zero early-vote rows by law** - Connecticut had no in-person early voting until 2024 - so that view is correctly empty, not missing.
- **Maryland's house district is rebuilt from `dbo.voterfile_2026`, because the absentee feed cannot express it.** MD elects delegates from **71** units: 29 whole legislative districts returning three delegates at large, plus 42 lettered subdistricts (01A, 01B, 27C, 47B ...). The feed has no subdistrict column - its `LegislativeDistrict` is the plain 1-47 district and is byte-identical to `SenateDistrict` - so the first MD pull dropped the 18 subdivided districts and shipped only 29. `voterfile_2026.StateLegLowerSubDistrict` carries the letter, joined on `RNC_Regid`, and MD's `hd_sql` / `extra_join` in `STATE_MODELS` now rebuild the full 71, ids matching DE's `md_house.json` exactly.
  - **The DISTRICT still comes from the feed; only the LETTER comes from the voter file, and only when the voter file agrees on the district.** A 2026 voter file describes where someone lives now, so for a voter who moved after the election being pulled it would silently reassign their ballot - the district disagrees for 8.8% of 2022 rows and 5.5% of 2024. Where it disagrees the historical subdistrict is unknowable and the row gets **no house district at all** (it still counts in the senate and statewide rollups).
  - **So MD house completeness is uneven, by design:** the 29 whole districts are exact, the 42 subdistricts hold **84.8%** of their voters in 2022 and **92.1%** in 2024. House totals run 93.1% / 96.2% of senate totals. Do not read a subdistrict count as a full turnout figure. Margins are barely affected - house R-61.5 vs senate R-61.3 in 2022, R-58.8 vs R-58.7 in 2024 - which is the check that the missing voters are not politically lopsided.
  - `StateLegLowerDistrict_Proper` is **empty** for MD, and the `*_PreviousElection` columns add only ~0.2pp of attribution, so neither is used. `hd_sql` / `extra_join` are honoured by **both** `state_query()` and `historical_query()`, so MD would behave the same way if it is ever activated for 2026. WV (100/17) and DE (41/21) need none of this.
- **Illinois' 2022 `SenateDistrict` column is corrupt and is rebuilt, not trusted.** IL nests two house districts per senate district - constitutional, Art. IV s.3 - and the feed confirms it holds for **100.0%** of IL rows in 2024 and 2026 but only **93.6%** in 2022, with the damage concentrated in SD 25 (18,757 rows) and SD 42 (14,090). That is a vendor defect, **not a redraw**: Illinois used one map all decade and its 2022 house column agrees with 2024 everywhere (no district retains under 91%). `illinois_senate_from_house()` plus `"senate_always_derived": True` on the IL model recomputes every senate district from the house one. When a row has no usable house district the senate is left **empty** rather than falling back to the rejected column - otherwise the bad values creep back in. The invariant to check after any IL pull is that **every senate district equals the sum of its two house districts, exactly**; it does, in both years.
- **The concentration test has a false-positive mode in dense urban states.** It flagged 15 IL house and 30 IL senate districts as "redrawn" between 2022 and 2024 even though Illinois never redistricted. Every one of those had **high** retention (91%+) and was caught by concentration alone: a Chicago district borders few enough neighbours that ordinary relocation piles into one of them, breaking the "movers scatter" assumption that holds in MI, GA and AK. **Use concentration only to promote a district that is already suspicious, never on its own**, and sanity-check against whether the state actually redistricted. RI came back clean on all four comparisons and needed no entries.
- **Stat gaps are real and expected in some states.** TX carries **no `RequestDate` at all** in either year, so its ABs-Requested view is all zeros in history while Returned and EV are fine — the mirror of RI, which is request-only. OR is all vote-by-mail, so **EV is legitimately zero** in both years, and 2022 additionally has no request dates. Neither is a pull failure; don't 'fix' them.

- **Safe to run in batches, but only since 2026-09-05.** The per-chamber files are per
  state, but `national.json`, `timeline.json` and `history.json` are per *year* and cover
  every state at once. The script used to rebuild those three from only the states in the
  current run — so a `--states NC` run would have deleted VA/WI/PA from both years'
  national + timeline files and from the index the site reads, leaving their per-chamber
  files orphaned on disk. It now merges onto what is already there, and rebuilds the index
  by globbing each year's directory, so the index always describes the files that exist.

- 2022 is filtered to `ElectionType = 'General Election'` (drops runoffs/spring/specials); 2024 is pulled unfiltered (appears general-only). Each is set per-year in `YEAR_CONFIG` (with the year's real cycle-start + election day, not the 2026 spring test dates).
- Prints diagnostics per state/year: ElectionType distribution, model match rate, NULL/0-district share, with hard flags for >5% NULL districts or a whole chamber of empty districts.
- **Caveat for display:** 2022/2024 were federal/presidential cycles — VA had no state-leg race those years, and WI 2022 ran under the pre-2023 map, so district geography won't align with the 2026 map for every state. Counts are real absentee/EV turnout by the voter's district, not legislative-race results.
- **Retired district lines.** Two lists, because a redraw is usually partial. `HISTORY_STALE_LINES` (year → a bare abbr, retiring both chambers, or `"ABBR:chamber"`, retiring one) is for redraws so broad that no district survived intact — currently `2022: ["VA", "WI", "NC"]`. `HISTORY_STALE_DISTRICTS` (year → `"ABBR:chamber"` → district ids) is for partial redraws and blanks **only the districts that moved**, which is almost always the right tool. **Michigan is the worked example, and it started life wrongly in the first list**: blanking all 110 house seats to protect 14 of them threw away comparable data for 96 districts. *Agee v. Benson* struck 13 districts and the remedial maps changed those plus the neighbours needed to rebalance. **House** ran Hickory in 2022 and Motown Sound A1 in 2024 and 2026; the change is confined to the contiguous Detroit block **1-14**, so 2022 is stale for those 14 and real for the other 96, and 2024 needs no entry at all. **Senate** ran Linden in 2022 *and* 2024 — four-year terms meant no 2024 senate election, so the feed still carried Linden that year — and Crane A1 is first used in 2026, differing in **14 of 38** (1, 2, 3, 5-11, 13, 23, 24, 38), so both past cycles are stale for exactly those and fine for the other 24. **The house list was derived from the voter file, not assumed**: join the 2022 and 2024 feeds on `RNC_RegID` and ask how many of a district's voters kept the same label. Districts 1-14 retain 0.3-67% against an 88-98% band for the rest. District 2 needs a second test — it retains 88.3%, inside the band, but 51% of what it lost went to district 1 alone, a boundary shift rather than the thin scatter ordinary relocation produces. The same test shows **no** redraw signal between 2022 and 2024 senate, confirming both are Linden. **MI's leg_2022 column was blanked statewide for a few hours on 2026-09-05 and is fixed.** DE's `LEG_REDISTRICTED` is all-or-nothing per year, so the `"MI": {2022}` entry added that morning wiped `leg_2022` from all 110 house and all 38 senate districts - including the 96 and 24 whose lines never moved. **Michigan is deliberately NOT in that list now.** The workbook already handles it district by district, carrying `leg_2022` for exactly the seats that survived the redraw, and it is more precise than the mechanism. Its house omissions (1-14) match the set independently derived from the voter file, which is good mutual confirmation of both. `legMarginCellsHtml()` renders a redrawn district's leg cell as **N/A** via `historyYearAppliesToDistrict()`, so it reads the same as the past-cycle ABEV cell beside it instead of going blank. **Two more partial redraws landed 2026-09-05.** **GA** was redrawn under the Dec 2023 court order: 60 of 180 house and 18 of 56 senate districts moved, so 120 house / 38 senate stay readable for 2022, and 2024 is fully comparable — the voter-file test shows 2024 → 2026 moving nothing at all (median retention 96.5% / 97.5%). **AK**'s 2021 board map was litigated through the 2022 election and finalized for 2024; the changes cluster in Anchorage (18-20, 22, 23) plus a pairing change at 3/4 and 7/8, leaving 31 of 40 house seats readable. AK senate ids are letters derived from house pairs, so the stale senate seats are exactly those covering a changed house seat (B, D, I, J, K, L). **AK districts 38 and 40 are deliberately NOT listed** despite 73-80% retention: they are rural seats with only 35-38 movers, and those movers scatter (11% top destination) rather than landing in one neighbour, so the low number is small-sample relocation, not a boundary change. **NJ, TX and OR needed no entries** — NJ verified clean in both directions (98% retention 2024 → 2026), TX and OR clean 2022 → 2024 with no 2026 feed rows to check against, which is worth re-testing when their feeds land. `historyYearAppliesToDistrict(year, joinKey, chamber)` drives the per-row N/A in both cell renderers, the sort key, and `trendYearsForScope()`; `historyYearAppliesToSelectedState(year, chamber)` still handles whole-chamber cases. **A null `joinKey` always applies** — statewide totals and the statewide trend are unaffected by redistricting, since a state's own borders do not move. `legRedistrictingNote()` fires on either list via `historyHasStaleDistricts()`. DE's `LEG_REDISTRICTED` carries `"MI": {2022}` (state-level there, which is correct: MI house keeps only `leg_2024` and MI senate has no 2024 election to drop);

## Shapefiles

`data/shapes/senate.zip` is **not** a stock Census file. It is
`cb_2024_us_sldu_500k` with Michigan's 38 districts replaced by the **Crane A1**
remedial plan - the map the MICRC adopted 2024-06-26 and the federal court
approved 2024-07-26 in *Agee v. Benson*, first used in the **2026** election.

Census cannot supply this yet. Its SLDU files are keyed to the legislative
session in effect (`LSY`), and Michigan senators elected in 2022 sit through
2026 under the old *Linden* map, so `cb_2024`, `cb_2025` and `tl_2025` all still
carry Linden. Crane A1 should appear in the 2027-session vintage.

Michigan geometry comes from the state's own Michigan Geographic Framework
layer, `Remedial_State_Senate_2021` (ArcGIS org `dxRQUfTDNtfqZ301`, owner
`michigan_admin`), pulled in NAD83 to match the Census `.prj` and simplified
with `mapshaper -simplify 8% keep-shapes` to ~20.4k vertices - the Census 500k
level (20.6k). Simplification is topology-aware, so shared borders stay
coincident: the 38 districts still tile with zero overlap.

Every non-Michigan feature is byte-identical to the Census original, and all
attributes are untouched, so `GEOID`/`SLDUST`/`NAMELSAD` joins are unaffected -
district *numbers* did not change, only boundaries. `ALAND`/`AWATER` are now
stale for Michigan; nothing reads them.

Rebuild only matters if the underlying Census file is refreshed - re-splice
rather than dropping in a new `cb_*` wholesale, or Michigan silently reverts to
Linden. To confirm which map a file holds: Crane A1 differs from Linden in
exactly 14 districts; the other 24 are identical. **This note used to enumerate them as 1, 2, 3, 5-11, 13, 23, 24, 38, and that list is almost certainly wrong.** The election workbook - which drives the `leg_2022` column and is the source of truth for it - omits 2022 margins for 1-11, 13, 23, 24 instead: the same count, agreeing on 12, but carrying SD 4 rather than SD 38. The workbook's set is a contiguous metro-Detroit block, which is what the *Agee v. Benson* remedy covered; the older list skips 4 in the middle of that block and reaches for 38, the Upper Peninsula, nowhere near it - it reads as a slip in a range written as "1, 2, 3, 5-11" where "1-11" was meant. `HISTORY_STALE_DISTRICTS` follows the **workbook**, so the ABEV and leg columns stay consistent district by district. Re-derive from the Crane A1 / Linden geometry if both files are ever to hand, and correct whichever is wrong.

## Data JSON Shapes

Chamber file (`data/abev/va_house.json`):
```json
{ "state_fips": "51", "state_abbr": "VA", "chamber": "house", "updated": "2026-07-18",
  "districts": [ { "district_id": "001",
    "requested": {"rep": 0, "dem": 0, "toss": 0},
    "returned": {...}, "ev": {...},
    "timeline": {"requested": [{"date": "2026-03-06", "rep": 0, "dem": 0, "toss": 0}], "returned": [...], "ev": [...]} } ] }
```
District `timeline` powers the chrono table in the district detail panel (`state.detailChronoMode` = daily/weekly, `state.detailChronoCumulative` = period vs running total).
- `national.json`: `{updated, states: [{state_fips, state_abbr, state_name, requested: {...}, returned, ev}]}` — authoritative statewide totals
- `timeline.json`: `{updated, states: {fips: {stat: [{date, rep, dem, toss}]}}}` — date keys are ISO dates plus `"earlier"` (first) and `"unknown"` (last). Powers the statewide chrono tables and the trend graph.
- `abev_files.json`: index consumed by the site (`house[]`, `senate[]`, `national`, `timeline`)
- A `"sample": true` flag on national/index would light the topbar SAMPLE DATA badge (no longer used; kept as a safety)

## District Explorer data (targets, incumbents, past leg margins)

Read at runtime from the sibling `RSLC-District-Explorer` project — **never copied into this repo**. Its per-chamber JSONs are generated from `data/State Legislative Election History.xlsx` by that project's `scripts/generate_chamber_jsons.py`; that workbook stays the single source of truth. Regenerate DE's JSON and this site picks it up on reload.

- `DE_DATA_BASES` in `config.js` is tried in order: `../RSLC-District-Explorer/data/` (local, requires serving from the shared parent folder) → `https://districts.rslc.gop/data/` (GitHub Pages fallback). The first base that answers is cached in `state.deBaseUrl`.
- Fetched lazily per `ABBR|chamber` into `state.deDataByKey` (Map of joinKey → DE record) on state select / chamber switch — not all ~99 files upfront.
- **File names come from DE's `chamber_files.json`** (cached in `state.deChamberIndex`), never guessed. Most files are `<abbr>_<chamber>.json`, but MI and MN are `michigan_*` / `minnesota_*` — guessing the name 404s and silently drops those states' targets, incumbents and past margins entirely. Falls back to the guessed name only if the index can't be fetched.
- **Sign flip:** DE stores Dem-positive margins; this project is GOP-positive. `legMarginRPositive()` negates on read. Don't skip this.
- **Tiers** come from the record's `tier` field (1–4; 1–3 in practice). **Offense/Defense is derived, not stored:** R-held = Defense, D-held = Offense, mixed multi-member = Split (`incumbentPartyCode()` → `targetSectionForParty()`).
- **Leg margin columns** are the two most recent `leg_YYYY` values present for that chamber, so odd-year states work (VA → 2023/2025; most → 2022/2024). If only one year exists and `LEG_REDISTRICTING_NOTES` has an entry for the state, the older year renders as an explicit N/A column plus a footnote — WI is the case in hand (redrawn 2023, so 2022 ran under a different map). Senate seats with staggered terms legitimately show N/A for a year they weren't up.

## State data exceptions (`STATE_DATA_NOTES`)

Every per-state data quirk that a reader could misread as a bug lives in
**`STATE_DATA_NOTES` in `modules/config.js`**, keyed by FIPS. It renders as a circled-ⓘ
next to the state's name in the national Overview table and beside the sidebar heading
when you're inside a state, reusing the Schedule tab's `.sched-info` component and its
delegated hover handler.

**This list is the contract: when you add or discover a state quirk, add it here.** A
caveat that lives only in a commit message, a code comment, or this file is invisible to
everyone actually looking at the numbers — which is the whole failure mode it exists to
prevent.

- **Keep the text to one phrase, two short sentences at the very most.** These render in a
  hover tooltip, where a paragraph does not get read. State the fact and stop — the
  reasoning, the measurements and the sources belong here in CLAUDE.md, not in the tooltip.
  Rewritten to that standard 2026-09-09 (31 notes, longest 100 characters); the smoke test
  asserts the budget so they cannot creep back.
- A note may carry `years`, `stats` and/or `chambers`; omitting a key means "all of them".
  `stateDataNotesFor(fips, {year, stat, chamber})` does the filtering. The state header
  narrows by chamber (so MD's house note doesn't appear over its senate); the national
  table passes no filter, so one dot covers everything known about that state.
- **Redistricting is deliberately NOT in this list.** It has its own machinery in
  `HISTORY_STALE_LINES` / `HISTORY_STALE_DISTRICTS` / `LEG_REDISTRICTING_NOTES`, which
  blanks the affected cells to N/A rather than merely annotating them. A note here would
  be a weaker response to the same problem.
- The tooltip handler is bound to **`.sidebar`, not `details`** — `#detailsTitle` is a
  *sibling* of `#details`, so a handler on `details` alone leaves the heading's icon inert.
- Currently 19 notes across 16 states. Most are on history-only states (TN, KY, TX, OR,
  IL, MD, CT, NY, MA, ND, SD), which is exactly why the state header carries the icon too:
  those states never appear in the national Overview table, so that dot alone would never
  be seen for them.

## UI filters

`Up in 2026` (`next_election === 2026`) and `Target Districts` checkboxes in the topbar. Both compute join-key sets in `refreshFilteredDistrictJoinKeySet()`.

**Map and table filter differently — two separate predicates:**
- `districtPassesActiveFilters()` drives the **map**: every active filter applies (intersection). Excluded districts stay drawn but dim (`districtBaseStyle`) and lose their number labels.
- `districtPassesTableFilters()` drives the **sidebar district table**: only Up-in-2026 removes rows. Target-district selection is deliberately map-only, so the table always keeps the full district list.

Inside the Target Districts section, clicking the heading toggles the whole mode, a section header (Defense/Offense/Split) toggles that section, and a tier cell toggles that tier — when targeting is off, the first such click turns it on scoped to just what was clicked. Sections/rows only dim while `targetDistrictsMode` is on; gating dimming on "is this section active" alone washes out the whole section whenever the mode is off.

## Key Concepts (inherited from District Explorer)

- **Join keys**: `"${stateFips}|${districtId}"` (e.g. `"51|001"`) everywhere. District IDs zero-padded to 3; from shapefiles use `SLDLST` (house) / `SLDUST` (senate) TIGER fields. Don't change this format.
- **State object**: all UI state in `modules/state.js`; mutate directly, then call render functions.
- **Render tokens**: `detailsRenderToken`, `districtNumberBuildToken`, `districtLabelRefreshToken` cancel stale async renders.
- **Nebraska**: unicameral — house view shows a "switch to Upper Chamber" message.
- **NH floterial districts**: NOT rendered (shapefile `nh_house_floterial.zip` copied but unused). Known gap vs District Explorer.
- **DC + territories**: filtered out of the map.

## UI Behaviors

- **Three views** (keys 1–3, topbar buttons + clickable sidebar cards): `ab` (Absentee Votes: Requested/Returned columns), `ev` (Early Votes), `abev` (ABEV Totals: Returned/EV/Total — the default). Each view maps to a coloring stat via `VIEW_MAP_STAT` (`returned`/`ev`/`voted`); map + column highlights follow it.
- **Margins are always percentages** of the stat total, positive = R. Two renderings: `formatNetPct` → "R+5.4"/"D+3.2"/"EVEN" (cards, hover, detail), and `marginCellHtml` → DE-style colored cell "+5.4"/"-3.2". Party buckets display as GOP / Dem / **Swing** (data key remains `toss`).
- District tables: raw count columns + margin cells, thin gap columns (`abev-gap-cell`) separating groups, sortable headers pre-split into two lines (count and margin columns sort independently). Headers break so the **year rides the first line with the stat name** ("2024 AB" / "Returned"), keeping each column as narrow as its longest word — `th` is `white-space: nowrap`, so the split in `viewColumnDefs` / `HISTORY_HEAD_LINES` is the only wrap. Both the full district table and the target tables carry a **Dist | Inc** pair; districts with no District Explorer record render Inc blank rather than "O".
- **Chrono views**: Districts / Daily / Weekly buttons in the statewide sidebar under the three stat cards (aqua `active-chrono` style; "Districts" = the plain district table), state mode only, built from `timeline.json` (statewide). Rows newest-first as M/D (weeks as "3/9-3/15", Mon–Sun, ends clamped to the window). Everything outside the state's ABEV window — before it opens (`ABEV_START_OVERRIDES`, now empty — the Sept 1 server-side cutoff supersedes it), after election day (`ELECTION_DAY_OVERRIDES`, default Nov 3), bad/unknown dates, pre-2026 — folds into a single **"Earlier"** row ("Unk" in district detail tables), which is the running-total baseline in cumulative so the newest row equals the all-time total. Switching chamber exits chrono.
  - **Cumulative is a sub-view, not a granularity**: an A/B box under Daily/Weekly toggles `state.chronoCumulative` / `state.detailChronoCumulative` between *Day-to-Day* (*Week-to-Week*) and *Cumulative*. `buildChronoRows(byDate, mode, earlierLabel, cumulative)` buckets by period first, then optionally accumulates — so weekly running totals fall out of the same path. Empty periods are dropped in period mode and kept in cumulative.
- **Past-cycle columns (2022 / 2024)**: **prepended** ahead of the current-cycle columns (e.g. `2022 ABEV Total | 2022 ABEV Margin | gap | 2024 …`), so tables read left-to-right 2022 → 2024 → 2026. Uses the current view's stat (`VIEW_MAP_STAT`), read from `data/abev/history/`. Every table that shows districts or dates gets them via `viewColumnDefs(view, {withHistory:true})` — full district table, target tables, and both chrono tables — so they stay in sync. States with no backfill get no columns at all (`historyAvailableForSelectedState()` guards `viewColumnDefs`), not a wall of "—".
  - **Historical ABEV box** (`None` / `On This Day` / `Final Results`) sits under the Districts/Daily/Weekly row and in the district detail panel. Quiet, vertically stacked, fixed-width `.option-box` styling shared with the cumulative A/B — deliberately lower-contrast than the chrono selector, because both are options on the view above rather than view switches. The width is fixed on purpose: sized to content, the box and its buttons resized whenever the note under them changed. In a chrono view the two boxes sit side by side in an `.option-box-row`. `state.historyMode` is shared across all three panels.
  - **Chrono views drop Final Results** (`historyModeFor({chrono:true})`): a finished total is one number, so it would repeat down every row. A Final chosen in the district table falls back to On This Day inside a chrono view and is restored on the way back.
  - **In chrono tables the alignment is per row**, not one snapshot: each row's own date span maps to the same days-out in the past year (`historyAlignedIso()`), summing that span in period mode or everything through its end in cumulative — so a 4/13–4/19 week in a VA cycle ending 4/21 compares against 10/31–11/6 of 2022. The "Earlier"/"Unk" row has no date to align and renders "—". Statewide chrono reads `history/<year>/timeline.json` (`ensureHistoryTimelines()`, one file per year, all states); district chrono reads the district record's own timeline. Which one is decided by the `joinKey` passed to `chronoTableHtml()` — null means statewide. **`none` is the default**, so the table opens without past cycles. *Final* = the stored all-time totals. *On This Day* = each past year's running total as of the same number of days before **its own** election day (`HISTORY_ELECTION_DAYS`: 2022-11-08, 2024-11-05), so a 102-days-out 2026 compares against 102-days-out 2022/2024.
  - `historyTotalsAsOf()` **excludes undated rows**, unlike the chrono views' "Earlier" baseline — an unknown-dated vote can't be placed in time, and using it as a baseline would report activity before the state's early-vote window even opened (VA 2022 has 29,353 undated but no real ABEV before Sept 19). Cost: at 0 days out On This Day runs under Final by exactly the undated count (~3% in VA). Expect **0** in both years until ~45 days out for VA.
- **Trend graph**: "Trend Graph" tab hangs from the map's upper-right corner (state mode only; faded/disabled in the plain district-table view, hidden nationally). Opens an SVG overlay panel: GOP/Dem/Swing lines (left axis, counts) + dashed net-margin% line (right axis), for the current scope (state chrono view or selected district), current view's stat, and the current granularity + cumulative setting. **Cycle checkboxes (2022 / 2024 / 2026)** pick which cycles to draw — past years are only offered where that scope has a backfill, and 2026 alone is on by default. Past cycles are **shifted onto the current cycle's calendar** by days-out from their own election day (`trendByDateForYear()`), so one x-axis serves all of them; they share the party colors and are told apart by line style (`trendYearStyle()`: 2026 solid, 2024 dashed, 2022 dotted, past years also lighter). Past cycles run the full domain since they're finished; only the current one stops at today. X-axis runs ABEV start → election day; "End graph at current date" checkbox (off by default) shortens it. Auto-closes when scope becomes unavailable (`updateTrendChartUi()`); hover crosshair + tooltip.
- Sidebar headers use proper chamber names from `data/state_chamber_names.json` (`"VA|house"` → "Virginia House of Delegates").
- National view: states colored by statewide net of the view's stat (no data = dimmed); sidebar table US totals row + per-state rows with all four stats (count + margin%), sortable, hover syncs map.
  - **Overview / Schedule toggle** (`state.nationalTab`, aqua `active-chrono` buttons) sits atop the national sidebar. **Overview** is the stat table above. **Schedule** (`nationalScheduleHtml()`) is a static reference table of every state's 2026 **general-election** ABEV windows — AB request deadline, AB return (ballots out → due), and early voting — as M/D dates, with circled-ⓘ tooltips (native `title`, so the scrolling sidebar can't clip them) for edge cases: postmark/late-arrival return deadlines, all-mail states, and county/municipal early-voting ranges. Rows carry `state-select-row`/`data-state-key` like Overview rows, so hover syncs the map and click enters the state (works for all 50, even no-data states). Tab is sticky across state enter/exit. Data is `modules/schedule.js` (keyed by FIPS), **generated by `scripts/build_schedule.py`** from NCSL tables (request/mail-out/return + early voting) computed against Nov 3 2026 and cross-checked against state SOS pages; re-run the script (or hand-edit the module) to change windows.
- Click district → detail panel: the same three view cards as the statewide overview (`viewCardsHtml(rec)`, showing that district's totals and switching view on click), then Total/GOP/Dem/Swing/Margin per stat, AB return rate = returned/requested, EV share, stacked bar, then its own chrono section. Chrono/history toggles there redraw the panel with `preserveScroll` — they sit well below the fold.
- Esc: close popup → deselect district → exit chrono → exit to national. Left Shift: toggle chamber. Ctrl+wheel: fine zoom.
- Auth is ON (`AUTH_ENABLED = true` in `modules/config.js`): password gate via the shared District Explorer Cloudflare Worker (`AUTH_WORKER_URL`). Bypassed automatically on `localhost`/`127.0.0.1`, so local dev is unaffected.
