# Credentials — never commit
scripts/db_config.ini

# Per-run skip-unchanged fingerprint cache (local optimization, not shared)
scripts/.abev_watermarks.json

# Python
__pycache__/
*.pyc

# OS
Thumbs.db
.DS_Store

# Claude Code local settings
.claude/settings.local.json
